"""
    make_michell_truss(h, n, L)

Generate a Michell truss given the distance between supports, `h`, the number of bars in the structure, `n`, and characteristic angle `gamma`
"""
function make_michell_truss(h, n, gamma)
    
    #empty vector of points
    points = Pt[]
    bars = Bar[]

    #YOUR CODE HERE

    return points, bars
end